# Bitácora de V5 — Automatizar el cuello de botella manual

> Una fila por ítem del backlog en `docs/backlog-v5-automatizacion.md`, lo
> más reciente arriba. Mismo espíritu que `docs/bitacora.md` (solo-añadir,
> breve, con lo que quedó pendiente), pero en archivo aparte: `bitacora.md`
> es el registro de Argos bajo el protocolo de `ENCARGO.md`; esta tanda la
> trabaja una sesión de Claude Code distinta, sin ese harness, a pedido
> explícito del usuario.

| Fecha | Ítem | Qué se hizo | Tests | Pendiente |
|---|---|---|---|---|
| 2026-09-09 | RS.28 | `src/notificador.ts`: `Notificador` (interfaz) + `NotificadorWebhook` (POST JSON a `NOTIFICADOR_URL`, `Authorization: Bearer` opcional con `NOTIFICADOR_API_KEY`; un estado no-2xx lanza `NotificadorError` con el cuerpo, sin incluir la clave en el mensaje) + `crearNotificadorWebhook()` (devuelve `null` sin URL configurada). `src/programadorImportacion.ts`: nuevo tipo `PaqueteProducido { id, conteos }`; `ManejadorDeReseñasNuevas` pasó de `Promise<void>` a `Promise<PaqueteProducido \| undefined>` para que quien procesa las reseñas nuevas pueda reportar qué produjo, y `ResultadoCicloNegocio` ganó el campo opcional `paquete` — el programador sigue sin saber qué es `empaquetar.ts` ni `AlmacenDeLotes`, solo transporta el dato. `src/servicio.ts`: dentro de `crearServidor`, el `alTerminarCiclo` compuesto ahora, además de llamar al `alTerminarCiclo` del usuario, recorre los resultados con `paquete` presente, genera un enlace real con `almacen.generarEnlace(...)` (mismo mecanismo de RS.18) y llama a `notificador.avisar(...)`; un fallo del aviso se atrapa con `.catch` y se escribe a stderr — nunca se propaga (avisar es best-effort). La URL base para el enlace se captura en una caja mutable (`urlBase`) porque no se conoce hasta que `server.listen` resuelve, y el aviso solo puede dispararse después de eso. Suite 247/247 verde con `tsc --noEmit`; sin `any`/`as`/`@ts-ignore`; sin red ni credenciales (la suite usa un notificador falso o espía `globalThis.fetch`, nunca sale a internet). Ajuste necesario en tests existentes: los manejadores falsos de `programadorImportacion.test.ts` y `catalogoNegocios.test.ts` que antes eran `async () => {}` (inferido `Promise<void>`) pasaron a `async () => undefined` para seguir cumpliendo la firma nueva — mismo comportamiento, tipo explícito. | `test/notificador.test.ts`: `NotificadorWebhook` arma el POST con el aviso completo, cabecera `Authorization` presente/ausente según haya clave, `crearNotificadorWebhook` con y sin `NOTIFICADOR_URL`, y un 429 se convierte en `NotificadorError` con el cuerpo pero SIN la clave en el mensaje. `test/servicioProgramador.test.ts` (RS.28): con un temporizador real de intervalo corto (30ms) y una espera real corta (250ms) — deliberado, es el único punto donde se comprueba el cableado real de `crearServidor`, no la lógica del ciclo (esa ya está cubierta sin timers en `programadorImportacion.test.ts`) — se verifica que el aviso llega con `placeId`, `nombreNegocio`, conteos correctos, y que el `enlaceUrl` avisado sirve de verdad para descargar el paquete; un notificador que rechaza no tumba el servidor (`/salud` sigue respondiendo); sin notificador configurado, el ciclo automático sigue sin fallar. `test/programadorImportacion.test.ts` ganó un test de que `ejecutarCicloUnaVez` propaga el `paquete` que devuelve el manejador. | Con RS.24-RS.28 cerrados, el backlog de automatización (`docs/backlog-v5-automatizacion.md`) queda completo. Lo que sigue es negocio, no ingeniería: conseguir el primer pago real (la métrica de `la-venta.md` §7) y, si se valida, elegir un proveedor de scraping real y probar el esquema de `TransporteImportacionReal` contra su API de verdad (sigue sin verificarse, ver nota de RS.24). |
| 2026-09-09 | RS.27 | `src/catalogoNegocios.ts`: `CatalogoNegocios` (interfaz) con `CatalogoNegociosEnMemoria` y `CatalogoNegociosSqlite` (tabla `negocios_programados(place_id PK, ficha_json, actualizado_en)`, `INSERT ... ON CONFLICT DO UPDATE` para el upsert). La ficha se persiste como JSON — evita inventar una columna por campo de `FichaNegocio` (incluido `sucursal` opcional); el `JSON.parse(...) as FichaNegocio` al leer cruza la frontera de un `unknown` que este mismo módulo escribió, declarado con su razón. `src/programadorImportacion.ts`: `registrarNegocio`/`quitarNegocio` pasaron de síncronos a `async` y escriben a través del catálogo (si hay uno, quinto parámetro opcional del constructor); nuevo método `cargarDesdeCatalogo()` que puebla el `Map` en memoria desde el catálogo, pensado para llamarse una vez al arrancar, antes de `iniciar`. `src/servicio.ts`: `programadorImportacion.catalogo` opcional; `crearServidor` pasó a `async function` para poder `await programador.cargarDesdeCatalogo()` antes de sembrar la lista `negocios` de las opciones (la lista explícita gana si un `placeId` se repite). Sin `catalogo`, el comportamiento es idéntico al de RS.26 (compatibilidad hacia atrás). Suite 237/237 verde con `tsc --noEmit`; declarado 1 `as` (frontera de `JSON.parse` propio). Ajuste en tests existentes: `test/programadorImportacion.test.ts` y `test/servicioProgramador.test.ts` pasaron sus llamadas a `registrarNegocio`/`quitarNegocio` a `await` (el cambio de firma no alteraba el comportamiento sin catálogo, pero comparar una `Promise` contra `true`/`false` en el assert de `quitarNegocio` sí era un bug real que `tsc` no detecta — `assert.equal` acepta `unknown`). | `test/catalogoNegocios.test.ts`: alta/baja/listado en memoria, upsert en memoria, `CatalogoNegociosSqlite` sobrevive a cerrar+reabrir (registrar, quitar, y upsert, cada uno en su propio archivo temporal), y el escenario completo de RS.27 — un `ProgramadorImportacion` registra un negocio con catálogo, se cierra todo, una instancia NUEVA sobre el mismo archivo parte con `listarNegocios()` vacío y lo recupera solo con `cargarDesdeCatalogo()`; análogo para `quitarNegocio` (no reaparece). `test/servicioProgramador.test.ts`: mismo escenario a nivel de `crearServidor` — cerrar el servidor y levantar uno nuevo sobre el mismo catálogo recupera el negocio sin pasar `negocios` en las opciones. | RS.28 (aviso automático al dueño) sigue en el backlog; el hueco es `alTerminarCiclo`, sin conectar todavía. No se tocó ningún test existente más allá de la corrección de `await` señalada arriba (ninguna aserción se debilitó, se corrigió una que comparaba mal). |
| 2026-09-09 | RS.26 | `src/programadorImportacion.ts`: `ProgramadorImportacion` recorre negocios registrados (`registrarNegocio`/`quitarNegocio`/`listarNegocios`, en memoria) a un intervalo fijo mediante un `Temporizador` inyectable (`TemporizadorReal` usa `setInterval` con `.unref()`, mismo patrón de sustitución que `TransporteHttp`/`TransporteImportacion`); si un negocio trae reseñas nuevas llama a `alProcesarNuevas(negocio, reseñas)`, inyectado desde fuera — el módulo no conoce `procesarLote` ni `empaquetar`. Un negocio que falla (la fuente rechaza o el callback lanza) queda como `{ ok: false, motivo }` y el ciclo sigue con los demás (mismo principio que RS.8). `src/servicio.ts`: `OpcionesServidor.programadorImportacion` arma el programador dentro de `crearServidor` (reutilizando `importador` como fuente y `empaquetarYGuardar` como callback — se le acotó la firma a `Pick<ContextoManejo, "redactor" \| "almacen">` para no arrastrar el resto del contexto HTTP), lo arranca al levantar el servidor y lo expone en `ServidorLevantado.programador`; `cerrar()` lo detiene. Sin `importador` configurado, se ignora (no hay de dónde importar). Suite 227/227 verde con `tsc --noEmit`; sin `any`/`as`/`@ts-ignore`; sin red ni credenciales (temporizador falso en los tests unitarios, `TemporizadorReal` solo se ejercita indirectamente vía disparo manual de ciclo en el test de integración). | `test/programadorImportacion.test.ts`: alta/baja/listado de negocios (incluida actualización de ficha al re-registrar el mismo `placeId`), un negocio con reseñas nuevas llama al manejador y uno sin novedades no, un negocio caído no cancela el ciclo para los demás (fuente que rechaza y callback que lanza, por separado), `iniciar` idempotente, `detener` cancela de verdad (disparo posterior no hace nada), `alTerminarCiclo` recibe los resultados tras un ciclo automático. `test/servicioProgramador.test.ts`: `crearServidor` con `programadorImportacion` deja el programador expuesto y corriendo con el negocio inicial, un ciclo manual procesa y el paquete queda deduplicado para una llamada posterior a `POST /lotes/importar`, registrar un negocio en caliente lo suma, sin `importador` no arranca nada, y `cerrar()` lo detiene. | RS.27 (catálogo persistente de negocios: hoy `registrarNegocio` vive solo en memoria del proceso, se pierde al reiniciar) y RS.28 (aviso automático al dueño: el hueco es `alTerminarCiclo`, que hoy nadie conecta a WhatsApp/email) quedan en el backlog. No se tocó ningún test existente. |
| 2026-09-09 | RS.25 | `src/servicio.ts`: nueva ruta `POST /lotes/importar` con body `{ placeId, ficha }`. `OpcionesServidor` gana el campo opcional `importador?: ImportadorReseñas`; sin él, la ruta responde 501 con motivo y el resto del servidor sigue igual. La lógica de armar y guardar el paquete (antes solo dentro de `manejarPOSTLotes`) se extrajo a `empaquetarYGuardar(reseñas, ficha, ctx)`, compartida ahora por `POST /lotes` (RS.14) y esta ruta — evita duplicar el tramo `procesarLote` → `resumirQuejas` → `exportarCSV`/`exportarMarkdown` → guardar. Sin reseñas nuevas responde 200 con `id: null` (nada que entregar no es un error); un `ImportadorError` del proveedor responde 502 con el motivo, sin tumbar el servidor. Suite 209/209 verde con `tsc --noEmit`. | `test/servicioImportador.test.ts`: sin importador configurado → 501 y `POST /lotes` sigue sirviendo en el mismo servidor; sin reseñas nuevas → 200 con `id: null`; camino feliz → procesa, arma paquete descargable por `GET /lotes/:id/paquete`, y una segunda llamada sobre el mismo negocio no reimporta (dedup end-to-end); proveedor caído → 502 con motivo y `/salud` sigue respondiendo después; validación de `placeId` faltante y de ficha inválida. | Nada del ítem. RS.26 (recurrencia/cron), RS.27 (alta de negocio sin repetir la ficha) y RS.28 (aviso automático) quedan en el backlog como siguiente fase; no se tocó ningún test existente. |
| 2026-09-09 | RS.24 | `src/importadorReseñas.ts`: `ImportadorReseñas` con `TransporteImportacion` inyectable (mismo patrón que `RedactorReal`), dedup por `idExterno` contra `AlmacenImportacion` (`AlmacenImportacionEnMemoria` / `AlmacenImportacionSqlite`), y `crearTransporteImportacionReal()` que lee `IMPORTADOR_URL` / `IMPORTADOR_API_KEY` del entorno (devuelve `null` si falta alguna). Un fallo del transporte se convierte en `ImportadorError` con motivo legible, nunca una excepción cruda. Reexportado desde `src/indice.ts`. | `test/importadorReseñas.test.ts`: dedup entre dos corridas sobre el mismo negocio, negocios distintos no comparten lo visto, reseñas incompletas (sin estrellas/fecha) entran marcadas igual que RS.1, transporte que lanza produce `ImportadorError`, persistencia SQLite sobrevive a cerrar y reabrir el almacén, `crearTransporteImportacionReal` sin credenciales devuelve `null`, `TransporteImportacionReal` parsea la respuesta del proveedor y descarta filas sin autor/texto, un 429 del proveedor no filtra la clave en el mensaje de error. | Nada del ítem. El esquema de respuesta asumido para `TransporteImportacionReal` (`review_id`/`author_name`/`review_text`/`review_rating`/`review_datetime_utc`) es el habitual en proveedores tipo Outscraper, pero no se ha verificado contra un proveedor real — igual que `RedactorReal` antes de RS.19, el contrato (`TransporteImportacion`) es lo estable; la clase concreta se sustituye si el proveedor elegido usa otro esquema. |
